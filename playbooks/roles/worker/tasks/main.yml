---
     - name: Join workers
       command: "{{ hostvars[groups['masters'][0]].worker_join_cmd }}"
       args:
         creates: /etc/kubernetes/kubelet.conf
       when: hostvars[groups['masters'][0]].worker_join_cmd is defined
  
            

